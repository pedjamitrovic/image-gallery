# image-gallery

To start the app navigate to root directory.

Backend:
1. Open cmd in root directory
2. cd back
3. npm start

Frontend:
1. Open cmd in root directory
2. cd front
3. npm start

There is no need to run npm install because it will be done automatically with npm start command.
